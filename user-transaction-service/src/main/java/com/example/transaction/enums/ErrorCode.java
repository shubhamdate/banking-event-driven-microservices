package com.example.transaction.enums;

public enum ErrorCode {

    LIMIT_EXCEEDED,
    DAILY_LIMIT_EXCEEDED,
    ACCOUNT_NOT_FOUND,
    INSUFFICIENT_FUNDS,
    DUPLICATE_REQUEST,
    INTERNAL_ERROR
}
